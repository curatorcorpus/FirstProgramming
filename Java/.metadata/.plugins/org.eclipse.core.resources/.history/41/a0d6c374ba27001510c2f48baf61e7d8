import java.util.Random;
import java.text.DecimalFormat;

public class PProject3 {
	
	public static void main(String args[]){
		// variable declaration
		int a, b, c;
		int d, e, f;
		int g, h, i, j;
		int test0, test1, test2;
		
		// object instantiation
		Random numGen = new Random();
		DecimalFormat set1 = new DecimalFormat("000");
		DecimalFormat set2 = new DecimalFormat("000");
		DecimalFormat set3 = new DecimalFormat("000");
		
		// phone number values
		a = numGen.nextInt(8);
		b = numGen.nextInt(8);
		c = numGen.nextInt(8);
		
		d = numGen.nextInt(8);
		e = numGen.nextInt(5);
		f = numGen.nextInt(3);
		
		g = numGen.nextInt(10);
		h = numGen.nextInt(10);
		i = numGen.nextInt(10);
		j = numGen.nextInt(10);

		test0 = numGen.nextInt(778);
		test1 = numGen.nextInt(743);
		test2 = numGen.nextInt(1000);
		
		
				"-" +set2.format(test1) + "-" + set3.format(test2));
		
	}
}
