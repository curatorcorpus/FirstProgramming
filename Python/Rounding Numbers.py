def powers(n):
    q = pow(n, 0)
    w = pow(n, 1)
    e = pow(n, 2)
    r = pow(n, 3)
    t = pow(n, 4)
    y = pow(n, 5)
    u = pow(n, 6)
    i = pow(n, 7)
    o = pow(n, 8)
    p = pow(n, 9)
    print q, w, e, r, t, y, u, i, o, p

print "enter a number for the first 10 powers"
n = input("The Number: ")

print powers(n)
